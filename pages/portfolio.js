import { Root, Header, Footer, WorkGallery } from "../components"

export default function Portfolio ( ){
    return (
        <Root>
            <Header />
            <WorkGallery />
            <Footer />
        </Root>
    )
}